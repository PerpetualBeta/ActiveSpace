// Intentionally empty. VirtualDisplayHelper was moved to a bundled
// helper process at Contents/Helpers/VirtualDisplayHost.app — see
// VirtualDisplay.swift. No Objective-C interop required by the main
// app at the moment; leaving this file in place so the Xcode build
// setting `SWIFT_OBJC_BRIDGING_HEADER` stays resolvable.
